'use client';
// Paginated, searchable, sortable list of saved WeatherRecords
// with bulk-select, delete confirmation modal, and inline edit
export function RecordList() {
  return <section className="flex flex-col gap-4" />;
}
