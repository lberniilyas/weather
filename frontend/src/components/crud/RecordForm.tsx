'use client';
// Create/Edit form for WeatherRecord using React Hook Form + Zod
// Validates location (geocoding), date range, and fetches weather on submit
export function RecordForm() {
  return <form className="flex flex-col gap-4" />;
}
